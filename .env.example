# Never commit a populated .env file. Goal 2A reads credentials only from the
# process environment; this file intentionally contains no credential value.

STOCK_AI_LOG_LEVEL=INFO
STOCK_AI_TIMEZONE=Asia/Tokyo

# Required only when running a live J-Quants V2 ingestion command.
JQUANTS_API_KEY=
